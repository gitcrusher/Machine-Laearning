1. eda.ipynb is the first file in which we are seeing that how to analyze the datset by visualising and seeing the datset type 
2. black_friday.ipynb is the file in which we haver done the basic data cleaning, encoding then we have also done train test split and then feature scaling as well .
3. flight_price_prediction.ipynb here we have understoop how to do eda in right way then how to scle the data properly and the conversion into numerical format is a slo taken care off in this file. Encoding like one-hot, label encoding has been taken care of 
4. titanic_feature_engineering.ipynb here we have see the independent variable dependent variables target variable, also we have seen MCAR(missing completely at random ),MAR(missing data at random )/Systematically missing values where we have learnt about different popular techniques 
    1. Mean/ MEdian Mode replacement -> disadvantage = distorting data
    2. random sample imputation -> advantage = no distoring data (bt cant be aplicable every where)
    3. Capturing nan values with  a new feature -> Note = works well when the data is not MCAR. Disadvantage = curse of dimensionality
    4. end of distribution imputation -> here so we gonna use the end dataset or in terms of normal distribution we will take the values after 3rd standard deviation and then replace it with the nan values. it caputres importance of missing values. Disadvantage = distortion and outlier issue 
    5. arbitrary imputation ->
    6. frequent categories imputation
5. house.ipynb 

